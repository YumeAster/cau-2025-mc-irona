import React from "react";
import SentenceGame from "./SentenceGame";

export default function SentenceGameTest() {
  return (
    <div style={{
      padding: 40,
      display: "flex",
      justifyContent: "center",
      alignItems: "center"
    }}>
      <SentenceGame />
    </div>
  );
}
